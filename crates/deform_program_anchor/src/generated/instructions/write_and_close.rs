//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::types::PlayerScore;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const WRITE_AND_CLOSE_DISCRIMINATOR: [u8; 8] = [193, 231, 239, 222, 142, 11, 112, 140];

/// Accounts.
#[derive(Debug)]
pub struct WriteAndClose {
    pub admin: solana_address::Address,

    pub lobby: solana_address::Address,

    pub creator: solana_address::Address,
}

impl WriteAndClose {
    pub fn instruction(
        &self,
        args: WriteAndCloseInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: WriteAndCloseInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.admin, true));
        accounts.push(solana_instruction::AccountMeta::new(self.lobby, false));
        accounts.push(solana_instruction::AccountMeta::new(self.creator, false));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = WriteAndCloseInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::ANCHOR_PROGRAM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct WriteAndCloseInstructionData {
    discriminator: [u8; 8],
}

impl WriteAndCloseInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [193, 231, 239, 222, 142, 11, 112, 140],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for WriteAndCloseInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct WriteAndCloseInstructionArgs {
    pub id: u64,
    pub scores: Vec<PlayerScore>,
}

impl WriteAndCloseInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `WriteAndClose`.
///
/// ### Accounts:
///
///   0. `[writable, signer, optional]` admin (default to `AdmiapAjgTDNTKHUAs7FWztVWSGPNU5zHv2YKSN5GiMg`)
///   1. `[writable]` lobby
///   2. `[writable]` creator
#[derive(Clone, Debug, Default)]
pub struct WriteAndCloseBuilder {
    admin: Option<solana_address::Address>,
    lobby: Option<solana_address::Address>,
    creator: Option<solana_address::Address>,
    id: Option<u64>,
    scores: Option<Vec<PlayerScore>>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl WriteAndCloseBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// `[optional account, default to 'AdmiapAjgTDNTKHUAs7FWztVWSGPNU5zHv2YKSN5GiMg']`
    #[inline(always)]
    pub fn admin(&mut self, admin: solana_address::Address) -> &mut Self {
        self.admin = Some(admin);
        self
    }
    #[inline(always)]
    pub fn lobby(&mut self, lobby: solana_address::Address) -> &mut Self {
        self.lobby = Some(lobby);
        self
    }
    #[inline(always)]
    pub fn creator(&mut self, creator: solana_address::Address) -> &mut Self {
        self.creator = Some(creator);
        self
    }
    #[inline(always)]
    pub fn id(&mut self, id: u64) -> &mut Self {
        self.id = Some(id);
        self
    }
    #[inline(always)]
    pub fn scores(&mut self, scores: Vec<PlayerScore>) -> &mut Self {
        self.scores = Some(scores);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = WriteAndClose {
            admin: self.admin.unwrap_or(solana_address::address!(
                "AdmiapAjgTDNTKHUAs7FWztVWSGPNU5zHv2YKSN5GiMg"
            )),
            lobby: self.lobby.expect("lobby is not set"),
            creator: self.creator.expect("creator is not set"),
        };
        let args = WriteAndCloseInstructionArgs {
            id: self.id.clone().expect("id is not set"),
            scores: self.scores.clone().expect("scores is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `write_and_close` CPI accounts.
pub struct WriteAndCloseCpiAccounts<'a, 'b> {
    pub admin: &'b solana_account_info::AccountInfo<'a>,

    pub lobby: &'b solana_account_info::AccountInfo<'a>,

    pub creator: &'b solana_account_info::AccountInfo<'a>,
}

/// `write_and_close` CPI instruction.
pub struct WriteAndCloseCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub admin: &'b solana_account_info::AccountInfo<'a>,

    pub lobby: &'b solana_account_info::AccountInfo<'a>,

    pub creator: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: WriteAndCloseInstructionArgs,
}

impl<'a, 'b> WriteAndCloseCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: WriteAndCloseCpiAccounts<'a, 'b>,
        args: WriteAndCloseInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            admin: accounts.admin,
            lobby: accounts.lobby,
            creator: accounts.creator,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.admin.key, true));
        accounts.push(solana_instruction::AccountMeta::new(*self.lobby.key, false));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.creator.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = WriteAndCloseInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::ANCHOR_PROGRAM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.admin.clone());
        account_infos.push(self.lobby.clone());
        account_infos.push(self.creator.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `WriteAndClose` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` admin
///   1. `[writable]` lobby
///   2. `[writable]` creator
#[derive(Clone, Debug)]
pub struct WriteAndCloseCpiBuilder<'a, 'b> {
    instruction: Box<WriteAndCloseCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> WriteAndCloseCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(WriteAndCloseCpiBuilderInstruction {
            __program: program,
            admin: None,
            lobby: None,
            creator: None,
            id: None,
            scores: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn admin(&mut self, admin: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.admin = Some(admin);
        self
    }
    #[inline(always)]
    pub fn lobby(&mut self, lobby: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.lobby = Some(lobby);
        self
    }
    #[inline(always)]
    pub fn creator(&mut self, creator: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.creator = Some(creator);
        self
    }
    #[inline(always)]
    pub fn id(&mut self, id: u64) -> &mut Self {
        self.instruction.id = Some(id);
        self
    }
    #[inline(always)]
    pub fn scores(&mut self, scores: Vec<PlayerScore>) -> &mut Self {
        self.instruction.scores = Some(scores);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = WriteAndCloseInstructionArgs {
            id: self.instruction.id.clone().expect("id is not set"),
            scores: self.instruction.scores.clone().expect("scores is not set"),
        };
        let instruction = WriteAndCloseCpi {
            __program: self.instruction.__program,

            admin: self.instruction.admin.expect("admin is not set"),

            lobby: self.instruction.lobby.expect("lobby is not set"),

            creator: self.instruction.creator.expect("creator is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct WriteAndCloseCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    admin: Option<&'b solana_account_info::AccountInfo<'a>>,
    lobby: Option<&'b solana_account_info::AccountInfo<'a>>,
    creator: Option<&'b solana_account_info::AccountInfo<'a>>,
    id: Option<u64>,
    scores: Option<Vec<PlayerScore>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
