//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::{BorshDeserialize, BorshSerialize};

pub const SET_INPUTS_DISCRIMINATOR: [u8; 8] = [61, 218, 101, 167, 7, 242, 192, 182];

/// Accounts.
#[derive(Debug)]
pub struct SetInputs {
    pub user: solana_address::Address,

    pub lobby: solana_address::Address,

    pub inputs: solana_address::Address,
}

impl SetInputs {
    pub fn instruction(&self, args: SetInputsInstructionArgs) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: SetInputsInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.user, true));
        accounts.push(solana_instruction::AccountMeta::new(self.lobby, false));
        accounts.push(solana_instruction::AccountMeta::new(self.inputs, false));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = SetInputsInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::ANCHOR_PROGRAM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct SetInputsInstructionData {
    discriminator: [u8; 8],
}

impl SetInputsInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [61, 218, 101, 167, 7, 242, 192, 182],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for SetInputsInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct SetInputsInstructionArgs {
    pub id: u64,
    pub batch_inputs_bytes: Vec<u8>,
}

impl SetInputsInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `SetInputs`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` user
///   1. `[writable]` lobby
///   2. `[writable]` inputs
#[derive(Clone, Debug, Default)]
pub struct SetInputsBuilder {
    user: Option<solana_address::Address>,
    lobby: Option<solana_address::Address>,
    inputs: Option<solana_address::Address>,
    id: Option<u64>,
    batch_inputs_bytes: Option<Vec<u8>>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl SetInputsBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn user(&mut self, user: solana_address::Address) -> &mut Self {
        self.user = Some(user);
        self
    }
    #[inline(always)]
    pub fn lobby(&mut self, lobby: solana_address::Address) -> &mut Self {
        self.lobby = Some(lobby);
        self
    }
    #[inline(always)]
    pub fn inputs(&mut self, inputs: solana_address::Address) -> &mut Self {
        self.inputs = Some(inputs);
        self
    }
    #[inline(always)]
    pub fn id(&mut self, id: u64) -> &mut Self {
        self.id = Some(id);
        self
    }
    #[inline(always)]
    pub fn batch_inputs_bytes(&mut self, batch_inputs_bytes: Vec<u8>) -> &mut Self {
        self.batch_inputs_bytes = Some(batch_inputs_bytes);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = SetInputs {
            user: self.user.expect("user is not set"),
            lobby: self.lobby.expect("lobby is not set"),
            inputs: self.inputs.expect("inputs is not set"),
        };
        let args = SetInputsInstructionArgs {
            id: self.id.clone().expect("id is not set"),
            batch_inputs_bytes: self
                .batch_inputs_bytes
                .clone()
                .expect("batch_inputs_bytes is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `set_inputs` CPI accounts.
pub struct SetInputsCpiAccounts<'a, 'b> {
    pub user: &'b solana_account_info::AccountInfo<'a>,

    pub lobby: &'b solana_account_info::AccountInfo<'a>,

    pub inputs: &'b solana_account_info::AccountInfo<'a>,
}

/// `set_inputs` CPI instruction.
pub struct SetInputsCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub user: &'b solana_account_info::AccountInfo<'a>,

    pub lobby: &'b solana_account_info::AccountInfo<'a>,

    pub inputs: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: SetInputsInstructionArgs,
}

impl<'a, 'b> SetInputsCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: SetInputsCpiAccounts<'a, 'b>,
        args: SetInputsInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            user: accounts.user,
            lobby: accounts.lobby,
            inputs: accounts.inputs,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.user.key, true));
        accounts.push(solana_instruction::AccountMeta::new(*self.lobby.key, false));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.inputs.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = SetInputsInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::ANCHOR_PROGRAM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.user.clone());
        account_infos.push(self.lobby.clone());
        account_infos.push(self.inputs.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `SetInputs` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` user
///   1. `[writable]` lobby
///   2. `[writable]` inputs
#[derive(Clone, Debug)]
pub struct SetInputsCpiBuilder<'a, 'b> {
    instruction: Box<SetInputsCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> SetInputsCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(SetInputsCpiBuilderInstruction {
            __program: program,
            user: None,
            lobby: None,
            inputs: None,
            id: None,
            batch_inputs_bytes: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn user(&mut self, user: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.user = Some(user);
        self
    }
    #[inline(always)]
    pub fn lobby(&mut self, lobby: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.lobby = Some(lobby);
        self
    }
    #[inline(always)]
    pub fn inputs(&mut self, inputs: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.inputs = Some(inputs);
        self
    }
    #[inline(always)]
    pub fn id(&mut self, id: u64) -> &mut Self {
        self.instruction.id = Some(id);
        self
    }
    #[inline(always)]
    pub fn batch_inputs_bytes(&mut self, batch_inputs_bytes: Vec<u8>) -> &mut Self {
        self.instruction.batch_inputs_bytes = Some(batch_inputs_bytes);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = SetInputsInstructionArgs {
            id: self.instruction.id.clone().expect("id is not set"),
            batch_inputs_bytes: self
                .instruction
                .batch_inputs_bytes
                .clone()
                .expect("batch_inputs_bytes is not set"),
        };
        let instruction = SetInputsCpi {
            __program: self.instruction.__program,

            user: self.instruction.user.expect("user is not set"),

            lobby: self.instruction.lobby.expect("lobby is not set"),

            inputs: self.instruction.inputs.expect("inputs is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct SetInputsCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    user: Option<&'b solana_account_info::AccountInfo<'a>>,
    lobby: Option<&'b solana_account_info::AccountInfo<'a>>,
    inputs: Option<&'b solana_account_info::AccountInfo<'a>>,
    id: Option<u64>,
    batch_inputs_bytes: Option<Vec<u8>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
