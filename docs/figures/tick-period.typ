// Standalone export of the adaptive client tick-period curve.
// The response is illustrative, using the model's 60 Hz defaults and P = 0.
#import "tick-model.typ" as tick
#set page(width: auto, height: auto, margin: 12pt, fill: white)
#tick.period-plot()
